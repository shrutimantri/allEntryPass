<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    tools:context=".MyActivity">

    <!--  Sample Dashboard screen with Two buttons -->
    <!--  Button to view all products screen -->
    <Button android:id="@+id/btnRegisterForEvent"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Register For Event"
        android:layout_marginTop="12dip"/>

    <!--  Button to create a new product screen -->
    <Button android:id="@+id/btnCreateStudentPass"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Create Student Pass"
        android:layout_marginTop="12dip"/>

    <!--  Button to create a new product screen -->
    <Button android:id="@+id/btnAddEvent"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Add Event"
        android:layout_marginTop="12dip"/>

</LinearLayout>
